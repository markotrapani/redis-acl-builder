# Redis ACL Builder - All Dependencies
# Single file for both development and production use

# === PRODUCTION DEPENDENCIES ===
# Core Flask web framework
Flask==3.0.0
Werkzeug==3.1.3

# Production WSGI server
gunicorn==23.0.0

# Flask core dependencies (explicit for clarity)
click>=8.0.0          # CLI support (Flask dependency)
itsdangerous>=2.0.0   # Security utilities (Flask dependency)
Jinja2>=3.0.0         # Template engine (Flask dependency)
MarkupSafe>=2.1.0     # Safe string handling (Jinja2 dependency)

# === DEVELOPMENT & TESTING DEPENDENCIES ===
# Testing framework
pytest==7.4.3
pytest-flask==1.3.0
coverage==7.3.2

# Development utilities
wheel>=0.37.0         # For building packages
setuptools>=65.0.0    # Package management