# Redis ACL Builder - Production Dependencies Only

# Core Flask dependencies (minimal for production)
Flask==3.0.0
Werkzeug==3.1.3
gunicorn==23.0.0

# Flask core dependencies (automatically installed but explicit for clarity)
click>=8.0.0          # CLI support (Flask dependency)
itsdangerous>=2.0.0   # Security utilities (Flask dependency)
Jinja2>=3.0.0         # Template engine (Flask dependency)
MarkupSafe>=2.1.0     # Safe string handling (Jinja2 dependency)