{
  "name": "redis-acl-builder",
  "version": "1.0.1",
  "description": "**Version v1.25.1-beta** - Optimization Box Persistence & Backend Error Fix",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "playwright test --config=tests/playwright.config.js",
    "test:ui": "playwright test --ui --config=tests/playwright.config.js",
    "test:headed": "playwright test --headed --config=tests/playwright.config.js",
    "test:report": "playwright show-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.56.0",
    "markdownlint-cli": "^0.45.0"
  }
}
