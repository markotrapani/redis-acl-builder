const ACLOptimizer={showOptimizationNotification(message,type){import('../core/utils.js').then(({default:Utils})=>{Utils.showNotification(message,type);});},async checkAndAutoOptimize(currentRule,currentVersion,currentMode,callbacks){if(!currentRule||!currentRule.trim()){return;}
try{const API=await import('../api/api-client.js').then(m=>m.default);const optimizeResponse=await API.optimizeRule(currentRule,currentVersion,currentMode);if(optimizeResponse.success&&optimizeResponse.savings>0){const optimizedRule=optimizeResponse.optimized_rule;if(callbacks.updateRuleText){callbacks.updateRuleText(optimizedRule);}
if(callbacks.updateLastGeneratedRule){callbacks.updateLastGeneratedRule(optimizedRule);}
const explanation=optimizeResponse.explanation||'';let message=`Auto-optimized: Saved ${optimizeResponse.savings} term${optimizeResponse.savings > 1 ? 's' : ''} (${optimizeResponse.original_term_count} → ${optimizeResponse.optimized_term_count})`;if(explanation&&(explanation.includes('grants no commands')||optimizeResponse.optimized_term_count===0)){message=`Auto-optimized: ${explanation} (${optimizeResponse.original_term_count} → ${optimizeResponse.optimized_term_count})`;}
this.showOptimizationNotification(message,'success');if(callbacks.syncFromRuleText){await callbacks.syncFromRuleText();}}}catch(error){console.error('Auto-optimization check failed:',error);}},applyCancelledAllOptimization(state,count,StateManager){state.grantedCategories.clear();state.blockedCategories.clear();state.grantedCommands.clear();state.blockedCommands.clear();state.orderedTerms=StateManager.removeAllCommandAndCategoryTerms(state.orderedTerms);this.showOptimizationNotification(`Auto-optimized: cancelled @all pattern simplified to empty ACL (${count} blocked categories removed)`,'success');},applyAllCategoriesOptimization(state,count,StateManager){state.grantedCategories.clear();state.grantedCategories.add('all');state.orderedTerms=state.orderedTerms.filter(term=>!(term.type==='category'&&term.operation==='grant'));StateManager.addTerm(state.orderedTerms,'category','grant','all');this.showOptimizationNotification(`Auto-optimized: replaced ${count} categories with "+@all"`,'success');},applyCancelExclusionOptimization(state,category,commands,count,StateManager){state.blockedCategories.delete(category);commands.forEach(cmd=>state.grantedCommands.delete(cmd));state.orderedTerms=StateManager.removeTermsByCategory(state.orderedTerms,category,'block');state.orderedTerms=StateManager.removeTermsByCommands(state.orderedTerms,commands,'grant');this.showOptimizationNotification(`Auto-optimized: removed cancelled "-@${category}" and ${count} individual commands`,'success');},applyNullCategoryOptimization(state,category,commands,count,StateManager){state.grantedCategories.delete(category);commands.forEach(cmd=>state.blockedCommands.delete(cmd));state.orderedTerms=StateManager.removeTermsByCategory(state.orderedTerms,category,'grant');state.orderedTerms=StateManager.removeTermsByCommands(state.orderedTerms,commands,'block');this.showOptimizationNotification(`Auto-optimized: null category "+@${category}" and ${count} individual command blocks cancelled out`,'success');},applyCategoryGrantOptimization(state,category,commands,count,StateManager){commands.forEach(cmd=>state.grantedCommands.delete(cmd));state.orderedTerms=StateManager.removeTermsByCommands(state.orderedTerms,commands,'grant');StateManager.toggleCategoryState(state,category,'grant');StateManager.addTerm(state.orderedTerms,'category','grant',category);this.showOptimizationNotification(`Auto-optimized: replaced ${count} commands with "+@${category}"`,'success');},applyCategoryBlockOptimization(state,category,commands,count,StateManager){commands.forEach(cmd=>state.blockedCommands.delete(cmd));state.orderedTerms=StateManager.removeTermsByCommands(state.orderedTerms,commands,'block');StateManager.toggleCategoryState(state,category,'block');StateManager.addTerm(state.orderedTerms,'category','block',category);this.showOptimizationNotification(`Auto-optimized: replaced ${count} command blocks with "-@${category}"`,'success');},async simplifyToCategory(category,state,StateManager,callbacks){const categoryCommands=await callbacks.getCategoryCommands(category);if(!categoryCommands||categoryCommands.length===0){console.warn(`No commands found for category ${category}`);return;}
categoryCommands.forEach(cmd=>{state.grantedCommands.delete(cmd);});state.orderedTerms=state.orderedTerms.filter(term=>!(term.type==='command'&&term.operation==='grant'&&categoryCommands.includes(term.value)));state.grantedCategories.add(category);state.orderedTerms.push({type:'category',operation:'grant',value:category});await callbacks.updateRuleText();callbacks.scheduleRender();import('../core/utils.js').then(({default:Utils})=>{Utils.showNotification(`Simplified: replaced ${categoryCommands.length} commands with "+@${category}"`,'success');});},async removeAllCategoryCommands(category,state,StateManager,callbacks){const categoryCommands=await callbacks.getCategoryCommands(category);if(!categoryCommands||categoryCommands.length===0){console.warn(`No commands found for category ${category}`);return;}
categoryCommands.forEach(cmd=>state.grantedCommands.delete(cmd));state.orderedTerms=StateManager.removeTermsByCommands(state.orderedTerms,categoryCommands,'grant');await callbacks.updateRuleText();callbacks.scheduleRender();import('../core/utils.js').then(({default:Utils})=>{Utils.showNotification(`Removed ${categoryCommands.length} individual @${category} commands`,'success');});},async detectCancelledAllPattern(state){const hasAllGranted=state.grantedCategories.has('all');if(!hasAllGranted)return null;const allCategoriesExceptAll=state.allCategories.filter(cat=>cat!=='all');const explicitlyBlockedCategoriesExceptAll=Array.from(state.blockedCategories).filter(cat=>cat!=='all');if(explicitlyBlockedCategoriesExceptAll.length===allCategoriesExceptAll.length&&explicitlyBlockedCategoriesExceptAll.length>=10){return{type:'cancelled-all',grantedCategory:'all',blockedCategories:explicitlyBlockedCategoriesExceptAll,count:explicitlyBlockedCategoriesExceptAll.length};}
return null;},async detectAllCategoriesPattern(state){const hasAllGranted=state.grantedCategories.has('all');if(hasAllGranted)return null;const allCategoriesExceptAll=state.allCategories.filter(cat=>cat!=='all');const explicitlyGrantedCategoriesExceptAll=Array.from(state.grantedCategories).filter(cat=>cat!=='all');if(explicitlyGrantedCategoriesExceptAll.length===allCategoriesExceptAll.length&&explicitlyGrantedCategoriesExceptAll.length>=10){return{type:'all-categories',categories:explicitlyGrantedCategoriesExceptAll,count:explicitlyGrantedCategoriesExceptAll.length};}
return null;},async detectCancelledExclusion(category,categoryCommands,state){const individuallyGrantedCommands=categoryCommands.filter(cmd=>state.grantedCommands.has(cmd));if(individuallyGrantedCommands.length===categoryCommands.length&&individuallyGrantedCommands.length>=3){return{type:'cancel-exclusion',category,commands:individuallyGrantedCommands,count:individuallyGrantedCommands.length};}
return null;},async detectNullCategory(category,categoryCommands,state){const individuallyBlockedCommands=categoryCommands.filter(cmd=>state.blockedCommands.has(cmd));if(individuallyBlockedCommands.length===categoryCommands.length&&individuallyBlockedCommands.length>=3){return{type:'null-category',category,commands:individuallyBlockedCommands,count:individuallyBlockedCommands.length};}
return null;},async detectFullCategoryGrant(category,categoryCommands,state){const individuallyGrantedCommands=categoryCommands.filter(cmd=>state.grantedCommands.has(cmd));if(individuallyGrantedCommands.length===categoryCommands.length&&individuallyGrantedCommands.length>=3){return{type:'grant',category,commands:individuallyGrantedCommands,count:individuallyGrantedCommands.length};}
return null;},async detectFullCategoryBlock(category,categoryCommands,state){const individuallyBlockedCommands=categoryCommands.filter(cmd=>state.blockedCommands.has(cmd));if(individuallyBlockedCommands.length===categoryCommands.length&&individuallyBlockedCommands.length>=3){return{type:'block',category,commands:individuallyBlockedCommands,count:individuallyBlockedCommands.length};}
return null;},async detectIndividualCategoryOptimizations(state,getCategoryCommands){const allCategoriesPattern=await this.detectAllCategoriesPattern(state);if(allCategoriesPattern)return[];const optimizations=[];for(const category of state.allCategories){const categoryCommands=await getCategoryCommands(category);if(!categoryCommands||categoryCommands.length===0)continue;if(state.blockedCategories.has(category)){const cancelledExclusion=await this.detectCancelledExclusion(category,categoryCommands,state);if(cancelledExclusion)optimizations.push(cancelledExclusion);continue;}
if(state.grantedCategories.has(category)){const nullCategory=await this.detectNullCategory(category,categoryCommands,state);if(nullCategory)optimizations.push(nullCategory);continue;}
const grantPattern=await this.detectFullCategoryGrant(category,categoryCommands,state);if(grantPattern){optimizations.push(grantPattern);}else{const blockPattern=await this.detectFullCategoryBlock(category,categoryCommands,state);if(blockPattern)optimizations.push(blockPattern);}}
return optimizations;},async checkAndAutoSimplifyCategories(state,detectMethods,applyMethod){if(!state.allCategories||state.allCategories.length===0){return;}
const optimizations=[await detectMethods.detectCancelledAllPattern(),await detectMethods.detectAllCategoriesPattern(),await detectMethods.detectIndividualCategoryOptimizations()].flat().filter(Boolean);await applyMethod(optimizations);},async applyOptimizations(optimizations,applyMethods,updateRuleText){if(optimizations.length===0)return;for(const optimization of optimizations){const{type,category,commands,count}=optimization;if(type==='cancelled-all'){applyMethods.applyCancelledAllOptimization(count);}else if(type==='all-categories'){applyMethods.applyAllCategoriesOptimization(count);}else if(type==='cancel-exclusion'){applyMethods.applyCancelExclusionOptimization(category,commands,count);}else if(type==='null-category'){applyMethods.applyNullCategoryOptimization(category,commands,count);}else if(type==='grant'){applyMethods.applyCategoryGrantOptimization(category,commands,count);}else if(type==='block'){applyMethods.applyCategoryBlockOptimization(category,commands,count);}}
if(optimizations.length>0){await updateRuleText();}}};export default ACLOptimizer;