import AppState from'../core/app-state.js';import DOMElements from'../core/dom-elements.js';import Storage from'../core/storage.js';import Utils from'../core/utils.js';import API from'../api/api-client.js';import DOMUtils from'../core/dom-utils.js';const RuleManager={setRule(rule){DOMElements.aclRuleInput.dataset.programmaticUpdate='true';DOMElements.aclRuleInput.value=rule;Storage.saveAclRule(rule);const inputEvent=new Event('input',{bubbles:true});DOMElements.aclRuleInput.dispatchEvent(inputEvent);Utils.executeMultipleEventHandlers([{method:'updateCharacterCounterProgrammatically',args:[DOMElements.aclRuleInput]},{method:'updateActionButtonStates',args:[]}]);this.parseRule();},async parseRule(skipRedundancyAnalysis=false){return this.parseRuleInternal(skipRedundancyAnalysis,true);},async parseRuleSilent(skipRedundancyAnalysis=false){return this.parseRuleInternal(skipRedundancyAnalysis,false);},async parseRuleInternal(skipRedundancyAnalysis=false,showErrorNotifications=true){if(AppState.isLoading)return;const rawRule=DOMElements.aclRuleInput.value.trim();const rule=Utils.normalizeACLRule(rawRule);if(!rule||rule.length===0){if(DOMElements.commandResults){DOMElements.commandResults.textContent='';}
if(DOMElements.resultsSummary){DOMElements.resultsSummary.style.display='none';}
if(!skipRedundancyAnalysis){this.hideRedundancyWarnings();}
return;}
if(rule!==rawRule){const cursorStart=DOMElements.aclRuleInput.selectionStart;const cursorEnd=DOMElements.aclRuleInput.selectionEnd;DOMElements.aclRuleInput.dataset.programmaticUpdate='true';DOMElements.aclRuleInput.value=rule;DOMElements.aclRuleInput.setSelectionRange(cursorStart,cursorEnd);Utils.executeEventHandler('updateCharacterCounterProgrammatically',DOMElements.aclRuleInput);}
const validation=await Utils.validateACLRule(rule);if(!validation.valid){const firstError=validation.errors[0];if(showErrorNotifications){Utils.showNotification(firstError,'error',5000);}
if(DOMElements.commandResults){Utils.showMessage(DOMElements.commandResults,`Invalid ACL syntax: ${firstError}`,'error');}
if(DOMElements.resultsSummary){DOMElements.resultsSummary.style.display='none';}
return;}
if(DOMElements.commandResults){Utils.showLoading(DOMElements.commandResults);}
try{const data=await API.parseRule(rule,AppState.currentVersion,AppState.currentMode);this.updateResultsSummary(rule,data);this.displayGroupedCommands(data.grouped_commands||{});}catch(error){if(showErrorNotifications){Utils.showNotification(`Server error: ${error.message}`,'error',5000);}
if(DOMElements.commandResults){Utils.showMessage(DOMElements.commandResults,`Error parsing rule: ${error.message}`,'error');}
if(DOMElements.resultsSummary){DOMElements.resultsSummary.style.display='none';}}finally{if(DOMElements.commandResults){Utils.hideLoading(DOMElements.commandResults);}}
if(!skipRedundancyAnalysis){try{this.analyzeRedundancy();}catch(error){console.error('Error starting redundancy analysis:',error);}}},async analyzeRedundancy(){const rule=Utils.normalizeACLRule(DOMElements.aclRuleInput.value.trim());if(!rule||rule.trim()===''){this.hideRedundancyWarnings();return;}
try{const response=await API.analyzeRedundancy(rule,AppState.currentVersion,AppState.currentMode);if(response.success&&response.analysis){await this.checkForOptimization(rule,response.analysis);}else{this.hideRedundancyWarnings();}}catch(error){console.error('Redundancy analysis failed:',error);this.hideRedundancyWarnings();}},async checkForOptimization(rule,redundancyAnalysis){try{const optimizeResponse=await API.optimizeRule(rule,AppState.currentVersion,AppState.currentMode);if(optimizeResponse.success&&optimizeResponse.savings>0){const hasBackendSuggestions=redundancyAnalysis.suggestions&&redundancyAnalysis.suggestions.length>0;const hasNoPermissionsWarning=redundancyAnalysis.warnings&&redundancyAnalysis.warnings.some(w=>w.includes('Rule results in no permissions'));const optimizesToEmpty=!optimizeResponse.optimized_rule||optimizeResponse.optimized_rule.trim()===''||optimizeResponse.optimized_rule==='(empty rule)';if(optimizesToEmpty&&hasBackendSuggestions){redundancyAnalysis.suggestions=[`Simplified rule: (empty rule)`];if(optimizeResponse.explanation){redundancyAnalysis.warnings=[optimizeResponse.explanation];}
this.displayRedundancyWarnings(redundancyAnalysis);return;}
if(hasNoPermissionsWarning){this.displayRedundancyWarnings(redundancyAnalysis);return;}
const simplifiedRule=optimizeResponse.optimized_rule||'(empty rule)';const isEmptyRule=!optimizeResponse.optimized_rule||optimizeResponse.optimized_rule.trim()==='';let optimizationSuggestion;if(isEmptyRule){optimizationSuggestion=`Simplified rule: ${simplifiedRule}`;}else{optimizationSuggestion=`Simplified rule: ${simplifiedRule}\nSaves ${optimizeResponse.savings} term${optimizeResponse.savings > 1 ? 's' : ''}`;}
if(!redundancyAnalysis.suggestions){redundancyAnalysis.suggestions=[];}
if(!redundancyAnalysis.warnings){redundancyAnalysis.warnings=[];}
const hasRedundancyWarnings=redundancyAnalysis.warnings&&redundancyAnalysis.warnings.some(w=>w.includes('Redundant inclusion')||w.includes('Redundant exclusion')||w.includes('Rule results in no permissions')||w.includes('Individual commands cover entire')||w.includes('Category'));if(optimizeResponse.explanation&&!hasRedundancyWarnings){redundancyAnalysis.warnings.push(optimizeResponse.explanation);}
redundancyAnalysis.suggestions=[optimizationSuggestion];redundancyAnalysis.has_redundancy=true;redundancyAnalysis.is_optimization=true;}
this.displayRedundancyWarnings(redundancyAnalysis);}catch(error){console.error('Optimization check failed:',error);this.displayRedundancyWarnings(redundancyAnalysis);}},displayRedundancyWarnings(analysis){const warningsContainer=document.getElementById('redundancyWarnings');const warningsList=document.getElementById('warningsList');const suggestionsList=document.getElementById('suggestionsList');if(!analysis.has_redundancy){this.hideRedundancyWarnings();return;}
warningsList.textContent='';suggestionsList.textContent='';const hasOptimization=analysis.suggestions&&analysis.suggestions.some(s=>s.startsWith('Saves ')&&s.includes('term'));const optimizesToEmpty=hasOptimization&&analysis.suggestions.some(s=>s.includes('(empty rule)')||s.includes('Simplified rule: \n')||s.includes('Simplified rule:  '));let filteredWarnings=analysis.warnings||[];const hasNoPermissionsWarning=filteredWarnings.some(w=>w.includes('Rule results in no permissions'));const hasEmptyRuleSuggestion=analysis.suggestions&&analysis.suggestions.some(s=>s.includes('(empty rule)'));const notGrantedWarnings=filteredWarnings.filter(w=>w.includes('Commands were not granted by earlier rules'));if(hasNoPermissionsWarning){filteredWarnings=filteredWarnings.filter(warning=>warning.includes('Rule results in no permissions'));}else if(hasEmptyRuleSuggestion&&notGrantedWarnings.length>0){const otherWarnings=filteredWarnings.filter(w=>!w.includes('Commands were not granted by earlier rules'));filteredWarnings=[...otherWarnings,notGrantedWarnings[0]];}
const hasRedundantTerms=filteredWarnings.length>0;const headerElement=warningsContainer.querySelector('.redundancy-header h4');if(headerElement){if(analysis.is_optimization||(hasOptimization&&!hasRedundantTerms)){headerElement.textContent='💡 Rule Optimization Available:';}else if(hasRedundantTerms){headerElement.textContent='⚠️ Redundant Terms Detected:';}else if(hasOptimization){headerElement.textContent='💡 Rule Optimization Available:';}else{headerElement.textContent='💡 Suggestions:';}}
if(filteredWarnings.length>0){filteredWarnings.forEach(warning=>{const warningDiv=document.createElement('div');warningDiv.className='warning-item';warningDiv.appendChild(DOMUtils.createTextWithBreaks(warning));warningsList.appendChild(warningDiv);});}
if(analysis.suggestions&&analysis.suggestions.length>0){analysis.suggestions.forEach(suggestion=>{const suggestionDiv=document.createElement('div');suggestionDiv.className='suggestion-item';if(suggestion.includes('Simplified rule:')){const parts=suggestion.split('Simplified rule: ');const formattedFirstPart=parts[0];const afterRule=parts[1].split('\n');const actualRule=afterRule[0];const additionalText=afterRule.slice(1).join('\n');const clickHandler=()=>{const simplifiedRule=actualRule.replace(/'/g,'');if(simplifiedRule==='(empty rule)'||simplifiedRule===''||simplifiedRule===' '){DOMElements.aclRuleInput.value='';}else{DOMElements.aclRuleInput.value=simplifiedRule;}
const inputEvent=new Event('input',{bubbles:true});DOMElements.aclRuleInput.dispatchEvent(inputEvent);Utils.executeEventHandler('updateCharacterCounterProgrammatically',DOMElements.aclRuleInput);this.parseRule();import('../components/interactive-acl-builder.js').then(({default:InteractiveACLBuilder})=>{if(InteractiveACLBuilder.state.isInitialized){InteractiveACLBuilder.syncFromRuleText();}});import('../handlers/event-handlers.js').then(({default:EventHandlers})=>{EventHandlers.updateActionButtonStates();});};const fragment=DOMUtils.createSimplifiedRuleSuggestion(formattedFirstPart,actualRule,additionalText,clickHandler);suggestionDiv.appendChild(fragment);}else{suggestionDiv.textContent=suggestion;}
suggestionsList.appendChild(suggestionDiv);});}
warningsContainer.style.display='block';},hideRedundancyWarnings(){const warningsContainer=document.getElementById('redundancyWarnings');if(warningsContainer){warningsContainer.style.display='none';}},updateResultsSummary(rule,data){if(!DOMElements.resultsSummary){return;}
DOMElements.resultsSummary.textContent='';if(rule===''){DOMElements.resultsSummary.appendChild(DOMUtils.createEmptyRuleSummary(data.total_available));}else{const percentage=data.impact_summary?data.impact_summary.overall_percentage:null;DOMElements.resultsSummary.appendChild(DOMUtils.createGrantedSummary(data.total_granted,data.total_available,percentage));}
DOMElements.resultsSummary.style.display='block';},displayGroupedCommands(groupedCommands){if(!DOMElements.commandResults){return;}
DOMElements.commandResults.textContent='';const sortedCategories=Object.keys(groupedCommands).sort();if(sortedCategories.length===0){DOMElements.commandResults.appendChild(DOMUtils.createNoCommandsMessage());}else{sortedCategories.forEach(category=>{const commands=groupedCommands[category];const section=DOMUtils.createCategorySection(category,commands,Utils.escapeHtml,Utils.formatNumber);DOMElements.commandResults.appendChild(section);});}}};export default RuleManager;